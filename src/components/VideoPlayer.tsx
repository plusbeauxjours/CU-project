import React from 'react';
import styled from 'styled-components/native';

const Text = styled.Text``;
export default ({url, landscapeCallback, portraitCallback}) => {
  return <Text>VideoPlayer</Text>;
};
